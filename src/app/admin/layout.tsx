import type{ReactNode}from"react";import{AdminShell}from"@/components/admin/admin-shell";import{requireRole}from"@/lib/portal-auth";import{adminCopy}from"@/i18n/admin-copy";import{getRequestLocale}from"@/lib/request-locale";
export const metadata={title:"Operations · Dar Tahara",robots:{index:false,follow:false}};export default async function AdminLayout({children}:{children:ReactNode}){const context=await requireRole(['staff','administrator']);const locale=await getRequestLocale();return <AdminShell canManageSettings={context.roles.includes('administrator')} copy={adminCopy[locale]} locale={locale}>{children}</AdminShell>}
