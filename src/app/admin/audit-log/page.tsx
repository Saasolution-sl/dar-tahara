import{AdminTable}from"@/components/admin/admin-table";import{requireRole}from"@/lib/portal-auth";import{serviceSelect}from"@/lib/supabase-rpc";import{adminCopy}from"@/i18n/admin-copy";import{getRequestLocale}from"@/lib/request-locale";export default async function Audit(){await requireRole(['administrator']);const copy=adminCopy[await getRequestLocale()];const c=copy.tables.auditLog;const rows=await serviceSelect<Array<{action:string;resource_type:string;resource_id:string|null;actor_user_id:string|null;created_at:string}>>('audit_logs?select=action,resource_type,resource_id,actor_user_id,created_at&order=created_at.desc&limit=500');return <AdminTable title={c.title} headers={c.headers} emptyLabel={copy.common.noRecords} rows={rows.map(r=>[new Date(r.created_at).toLocaleString(),r.actor_user_id||'system',r.action,r.resource_type,r.resource_id||'—'])}/>}
